# BasketballLife V8.1 修正交接

- 階段：maintenance／隔離修正分支 `codex/v81-review-fixes`
- 目標：修正 V8.1 Review 發現的市場報價、事件節奏、NBA／歐洲迴圈、玩家姓名顯示與排行榜名次風險；不改版本號與 README。存檔復原提案已取消，維持原有存檔行為。
- 已完成：正式市場報價不再由候選補足；特殊事件最多三件並新增最後一舞取向；40 歲以上已有 NBA 履歷者不再由歐洲回 NBA；動態姓名 HTML escape；一般賽季固定有一場本季關鍵戰，國家隊徵召時由國家隊關鍵戰優先接管。
- 關鍵戰內容：依上季賽事、球隊方向、宿敵、合約年與傷病生成背景；提供搶代表作、以球隊勝負為優先、控制負荷三種打法；結果寫入 `seasonHistory.keyBattle`，影響代表戰／失利故事、宿敵尊重、疲勞與身體負荷，以及職業球探市場評估。最後一舞仍保留作為真正生涯關卡。
- 本次補強：一般／國家隊關鍵戰在選擇前顯示與實際判定公式一致的預估成功率、表現值範圍與國家隊預估獎勵點；排行榜卡片與退休頁都改由 API 回傳完整全球榜的 `global_rank`，不再用前 50 名加本人資料推算；已存在的退休存檔每次確認公開生涯時也會刷新舊名次。
- 重要檔案：`js/career/contract-engine.js`、`js/career/season-engine.js`、`js/events/event-engine.js`、`js/ui/career-view.js`、`js/ui/event-view.js`、`js/ui/growth-preview.js`、`js/ui/retirement-view.js`、`tests/v81-behavior.test.mjs`、`tests/v81-integrity.test.mjs`
- 驗證：存檔恢復修改已撤回；46/46 tests；變更 JS `node --check`；`git diff --check` 通過。既有 HTTP 瀏覽器矩陣已走過 390px、430px、1440px 的首頁→開始生涯→季初訓練，主要行動按鈕可見且可點，遊戲內容沒有可見左右溢出。另以固定 Seed 走過 390px 的真實傷病分支，確認健康頁會顯示傷勢、部位、嚴重度、缺席場數、恢復時間與舊傷紀錄，季末結算後可正常進入下一季。以正常長生涯推進至 2039 年、29 歲、韓國職業，實際觸發成人代表隊邀請；接受後進入「國家隊關鍵戰」，選擇團隊打法後完成冠軍、國際賽數據、疲勞與傷病結果。本次 HTTP 預覽回應 200，手機版載入 34 個統一版本資產。
- 跨裝置修正：`index.html` 的本機 CSS、JS、資料與 manifest 資產統一改用 `8.1.0-sync2`，避免桌機與手機因舊 query cache 載入不同版本；手機事件選項強制保留成功率標籤，能力點數值與說明也強制可見。
- PR #34 狀態：`nationalTeamOpportunity()` 先判定徵召，只有成功進入國家隊特殊事件後，玩家選擇接受徵召才會進入 `showNationalKeyBattle()`；不是每季固定出現的事件。現有測試只驗證函式與文案存在，未覆蓋實際「徵召 → 關鍵戰」流程，因此玩家可能長時間看不到它。
- 目前風險：國家隊徵召仍是條件式出現，不是每年保證出現；完整全球名次依賴 Online API 回傳的排行榜 metadata。本機 HTTP 預覽的 Online API 404 為預期警告，本機頁面本身回應 200。瀏覽器 QA 產生的測試生涯只存在本機測試存檔，未上傳。
- 下一步：合併舊存檔名次刷新修正；之後觀察正式版手機快取更新與退休／排行榜名次是否恢復正常，不加入存檔自動修復。

## V8.1.1 完整實作交接

- 階段：maintenance／目前工作分支；未上傳、未部署、未修改 `main`。
- 目標：把 V8.1.1 公告中尚未落地的球員評估內容完成：八項主要能力維持可培養與存檔相容，新增由既有能力、位置、角色、表現與健康狀態推導的細分能力、球員特性、成長方向、情境效果與球隊需求評估。
- 已完成：三分、接球投籃、持球投籃、中距離；切入、對抗、低位、手感；外圍、內線、協防、護框等細分能力可由 `js/career/ability-profile.js` 推導。符合門檻時會顯示三分空間點、持球創造者、低位終結者、外圍防守者、護框保護者等特性與成長方向。
- 已接入：賽季數據與球探解讀會依球員特性與比賽情境產生小幅表現差異；自由市場與合約會同時參考球員定位、特性、球隊方向與需求，合約卡會顯示球隊需求、已形成特性與成長方向；能力頁會顯示細分能力面板。沒有新增必填的主要存檔能力欄位。
- 相容性：舊 V8.1.0／V8.1.1 生涯、退休、公開生涯、排行榜與既有主要能力資料不需重玩；V9.0 七級 Seed 天賦分布與養成重製未納入本次變更。
- 重要檔案：`js/career/ability-profile.js`、`js/career/season-engine.js`、`js/career/contract-engine.js`、`js/ui/career-view.js`、`css/career.css`、`tests/v81-behavior.test.mjs`、`tests/v81-integrity.test.mjs`。
- 驗證：51/51 tests；變更 JavaScript `node --check`；`git diff --check` 通過；本機 HTTP 首頁回應 200。瀏覽器未進入既有本機存檔的能力頁，以避免改動使用者測試存檔；本次完整流程仍以單元／完整回歸測試驗證。
- 目前風險：細分能力與情境效果是可調參數，仍需正式玩家數據觀察平衡；本機 Online API 404 是預覽環境預期警告。此次 `git fetch origin main` 受工作樹 `FETCH_HEAD` 權限錯誤阻擋，因此本地可讀的 `origin/main` 已檢查，未宣稱完成最新遠端抓取。
- 下一個最安全任務：先在本機預覽與玩家測試中觀察不同角色、老將與自由市場報價，再由使用者明確授權後安排上傳；不要在未授權前部署或修改 D1。

## V8.1.1 舊存檔續約流程修正

- 問題：部分未包含 `injuryHistory` 的舊存檔在合約到期進入自由市場時，`scoutingScore()` 直接讀取 `.length`，造成續約畫面顯示 `Cannot read properties of undefined`。
- 修正：市場評價將缺少的傷病履歷視為空陣列；不改變有傷病紀錄玩家的原本扣分公式，也不新增或刪除存檔資料。
- 驗證：新增舊存檔相容性回歸測試；針對性測試 24/24 通過，完整測試與 HTTP 預覽需於本次收尾後重新確認。
- 後續定位：實際瀏覽器堆疊確認合約卡把角色資料誤當成細分能力資料，已分離 `playerRoleProfile()` 與 `v811AbilityProfile()`；同時以 `8.1.1-sync2` 資產版本清除舊快取。對既有「QA國家隊測試」生涯重新載入後，2047 合約到期頁已顯示母隊續約與自由市場按鈕，沒有錯誤提示。
- 最終驗證：53/53 tests；變更 JavaScript `node --check`；`git diff --check` 通過；本機 HTTP 回應 200，瀏覽器使用 `8.1.1-sync2` 的合約腳本並成功恢復續約畫面。

## V8.1.1 負荷標籤與訓練面板修正

- 設計修正：年齡不再直接提供職業球員的上場時間上限。`veteranMinutesProfile()` 的分鐘限制只由體能、耐久、健康、身體負荷、出勤、近期產出、傷病與角色共同決定；健康且 readiness 足夠的 50 歲體能型球員可取得完整 36 分鐘模擬上限。
- 玩家文字：移除「智慧輪替管理」與「老將負荷管理」的年齡導向標籤，改為「正常主力輪替」、「依對位輪替」或有實際風險時的「身體狀態管理」。
- UI 修正：訓練能力面板改以完整 `.trainingPanel` 容器更新，避免每次擲骰／返回都重複插入細分能力面板；生涯摘要同步移除整個訓練容器。
- 舊存檔相容：續接 `stage:"training"` 時不再直接還原舊版 HTML，改用保存的骰子、已使用狀態、訓練進度與揭曉狀態重建目前單一面板；沒有重骰、不改能力值、不刪存檔。
- 快取修正：資產 query 版本由 `8.1.1-sync2` 更新為 `8.1.1-sync4`，確保瀏覽器與 Service Worker 載入舊存檔重建修正，而不是沿用舊版面板程式。
- 流程修正：舊存檔若骰子已全部分配，重建後會恢復「進入本季事件 →」按鈕，同時保留「返回上一步」。
- 驗證：新增舊存檔訓練面板與完成按鈕的完整性檢查；完整 55/55 tests、相關 JavaScript `node --check`、`git diff --check`、本機 HTTP 200，並以現有本機存檔實測 `trainingPanel=1`、`derivedAbilityPanel=1`、完成按鈕可見通過。

## V8.1.1 關鍵戰成功率校準

- 問題：一般關鍵戰用固定 62 分門檻搭配絕對 OVR 計算，造成 HBL／低層級球員三個選項可能全部 0%，高 OVR 職業球員又可能全部 100%。
- 修正：改以所在聯賽的能力基準計算相對表現，並保留打法、關鍵能力、近期狀態、健康、球隊方向與市場評價的影響；預覽與實際結算仍共用同一個表現公式。
- 相容性：不改存檔欄位、既有關鍵戰紀錄、排行榜或 D1；舊生涯重新進入尚未選擇的關鍵戰時會直接使用新公式。
- 快取修正：首頁全部本機資產 query 由 `8.1.1-sync4` 統一更新為 `8.1.1-sync5`，避免頁面網址已更新但關鍵戰仍載入舊版事件程式。
- 驗證：新增 HBL 與 CBA 高低端回歸測試；完整 56/56 tests、相關 JavaScript `node --check`、`git diff --check` 通過。
- 狀態：只在目前工作分支修改，未推送、未建立 PR、未合併、未部署。V9.0 暫存改動已全部取消。

## V8.1.1 關鍵戰目標定義修正

- 問題：三種打法原本都把「表現值達到 62」顯示成預估成功率，導致全力打法的數字其實不是代表作機率，控制負荷也會因個人表現較低而被錯誤顯示成低成功率。
- 預覽：全力打法分別顯示代表作機率與關鍵貢獻機率；團隊打法顯示守住關鍵戰機率；控制負荷不顯示虛構百分比，改為明確顯示必定生效的疲勞、身體負荷與後續傷病風險效果。三種打法都保留預估表現值。
- 結算：新增不破壞舊資料的 `objective`、`objectiveSuccess`、`performanceOutcome` 欄位，將策略目標與場上表現分開記錄；舊 `outcome`、排行榜與既有生涯紀錄格式繼續保留。
- 快取：首頁本機資產 query 由 `8.1.1-sync5` 統一更新為 `8.1.1-sync6`。
- 本機實測：既有 HBL、17 歲、OVR 44 存檔顯示「代表作 0%／關鍵貢獻 81%」、「守住關鍵戰 53%」與「負荷控制必定生效」，沒有選擇方案或推進存檔。
- 狀態：只在目前工作分支修改，未推送、未建立 PR、未合併、未部署。

## V8.1.1 關鍵戰個人／團隊雙軌模型

- 比較方式：一般關鍵戰三張卡統一顯示「球隊勝率、代表作機率、關鍵貢獻以上機率」，移除玩家無法對應實際結果的表現值區間。
- 球隊勝率：獨立參考聯賽相對能力、位置所需的傳球／球商／防守／籃板等團隊技能、近期事件表現、信心、健康、球隊方向與評價；團隊打法提供最高戰術加成，全力打法不保證球隊勝率最高，控制負荷會犧牲部分勝率。
- 個人表現：仍由聯賽相對能力、關鍵能力、近況與健康決定；代表作門檻由過度嚴苛的 78 校準為 74。三種打法使用不同波動範圍，全力打法波動最大並保留合理的爆發可能，團隊打法較穩定，控制負荷波動最小且個人上限較低。
- 實際結算：個人表現與球隊勝負使用分離且可重現的 Seed 結果；球員可以個人表現普通但靠團隊贏球，也可能打出代表作卻輸掉比賽。市場與宿敵評價看個人表現，團隊目標看實際勝負，負荷控制效果仍確定生效。
- 相容性：既有已完成關鍵戰紀錄、`outcome`、舊存檔、退休資料、公開生涯與排行榜格式不變；新紀錄額外保留 `teamWinChance` 與 `teamWon`。
- 快取：本機資產 query 由 `8.1.1-sync6` 更新為 `8.1.1-sync7`。
- 驗證：59/59 tests、`node --check js/events/event-engine.js`、`git diff --check` 通過；取消的 V9.0 檔案維持零差異。平均 HBL 測試模型顯示全力 44% 球隊勝率／25% 代表作／63% 關鍵貢獻以上，團隊 52%／4%／47%，控制負荷 38%／0%／16%。
- 狀態：只在目前工作分支修改，未提交、未推送、未建立 PR、未合併、未部署。

## V9.0 七級 Seed 與專精養成重製

- 階段：production／本機 Preview 2；工作分支 `codex/v90-seed-progression`，未上傳、未合併、未部署。
- 核心改動：新 V9.0 生涯改用 SSS+、SS+、S+、S、A、B、C 七級專精天賦模型。高級 Seed 提高總上限、核心適性與頂尖能力數量，不再把固定加成平均灌入八項能力；SSS+ 可同時擁有多項 99 上限，但仍保留能力差異。
- 角色分布：加入組織核心、持球得分手、籃板後衛、攻守側翼、持球側翼、組織前鋒、衝框前鋒、外線型長人、組織中鋒、護框核心、低位策應核心與全能串聯者。位置只影響出現傾向，不鎖死球員角色。
- 養成：V9 核心／延伸適性影響 29 歲前的訓練成本與一鍵推薦分配；非適性能力仍可訓練、累積點數及突破。V8.1 舊生涯保留原本的全能力 Seed 成長曲線。
- 玩家介面：能力頁顯示天賦型態、核心適性、延伸適性與養成說明；退休 Seed 揭露同步顯示該球員的 V9 天賦結構。八項主要能力與 V8.1 細分能力分析維持不變。
- 存檔相容：V9 新生涯寫入 `careerVersion: 9.0.0`、`talentVersion: 1` 與 `talentProfile`；讀取 V8.1 舊存檔時不重抽、不重算、不覆寫原有 stats、caps、growth 或已完成生涯資料。
- 排行榜：新增獨立 `v9` 排行時代與 V9 專用每週 Seed ID；V8.1、V8.0、V7.50 保留為舊版本冠軍榜與個人公開生涯。V9 客戶端仍能用 V8.1 驗證格式重新公開合法舊存檔。
- D1：新增加法式 migration `migrations/0004_v9_leaderboard_era.sql`，只建立 `v9` 統計列，不刪除或改寫任何舊排行榜資料；本機尚未套用正式 D1。
- 驗證：V9 七級各 600 組分布、跨位置 1200 組角色可達性、V8.1 舊存檔、訓練成本與 V9／V8.1 排行分流均有回歸測試；完整 67/67 tests、相關 JavaScript `node --check`、`git diff --check` 通過。本機 HTTP `http://127.0.0.1:4174/?v=9.0.0-preview2` 已實際建立 SSS+ 生涯、完成一鍵訓練並確認天賦面板正常顯示。
- 目前風險：七級數值仍需要玩家實玩確認長生涯平衡；尚未做七級各自的完整退休模擬，也未對正式 D1 或正式排行榜做寫入測試。正式站與 `main` 都未變更。
- 下一個最安全任務：先由使用者檢查 Preview 2 的天賦分布與養成手感，再針對過強／過弱級別做單次集中校準；取得明確授權前不要推送、合併或部署。

## V9.0 玩家文字與頂部／養成 UI 重整

- 階段：alpha／本機預覽；只修改目前 `codex/v90-seed-progression` 分支，未上傳、未合併、未部署。
- 玩家文字：移除玩家畫面的本機預覽、README、session、API 原始錯誤與版本修補說明；天賦、合約、連線、BL LIVE 與錯誤提示改成玩家能直接理解的結果文字。
- 頂部 UI：390／430px 保留重新開始、排行榜與玩家暱稱三個 44px 操作；版本與連線狀態不再佔用手機首要視線。球員卡改為一列身份加一列四項數值，本季六階段改成單一「目前進度」提示，職業資訊維持橫向可滑動。
- 養成 UI：天賦型態與核心／延伸適性優先顯示；八項能力與場上技術預設收合但可完整展開。推薦分配維持原有邏輯與返回調整，修正骰子全部揭曉後提示仍停在 4/5 的顯示問題。
- 相容性：沒有變更 Seed 數值、骰子結果、能力點、合約計算、存檔欄位、排行榜格式或 D1。
- 驗證：68/68 tests；相關 JavaScript `node --check`；`git diff --check`。本機 HTTP 實測首頁→建立生涯→季初特訓→展開兩個詳細面板→推薦分配→顯示「進入本季事件」。390／430px 均無水平溢出，球員卡 110px 高，頂部主要按鈕 44px。
- 目前風險：尚未檢查完整職業合約頁與退休頁的新文字在真實長生涯中的視覺效果；正式站與正式 D1 未測試也未變更。
- 下一個最安全任務：先由使用者檢查目前本機預覽的資訊層級與用詞，再決定是否繼續做退休頁 progressive disclosure；未取得明確授權前不要上傳。

## V9.0 生涯轉播 UI 垂直切片

- 階段：alpha／本機預覽；延續 `codex/v90-seed-progression`，未上傳、未合併、未部署。
- 視覺方向：首頁改為籃球生涯封面與浮接式建角卡；遊戲內改為球員證、目前進度與章節舞台；一般事件改為編號決策卡；季初養成改為天賦卡、骰子與推薦分配組成的訓練控制台。保留 BasketballLife 黑、橘、金與深藍配色，不複製參考站的品牌或素材。
- 玩家流程：快速建立與完整自訂維持原本功能，但資訊層級與主要行動更集中；快速／完整建立面板不會同時顯示。事件結果與下一步按鈕、訓練詳細能力與場上特性仍可正常展開和操作。
- 相容範圍：本次只新增首頁／進行中生涯的顯示狀態 class 與最後一層 CSS；未變更 Seed、事件機率、能力計算、存檔欄位、排行榜、退休、公開生涯、D1、版本號或 README。
- 瀏覽器驗證：本機 HTTP `http://127.0.0.1:43122/?v=v9-broadcast-shell3` 已走過首頁、快速開始、季初特訓、一般事件、事件結果與完整自訂。390、430、720、1080px 均無頁面水平溢出；手機主要開始按鈕 62px、事件選項至少 112px、下一步 56px。
- 自動驗證：68/68 tests；`node --check js/ui/growth-preview.js`；`git diff --check` 通過。
- 目前風險：完整職業合約、退休與排行榜頁仍使用既有內容骨架；真實手機瀏覽器的 safe-area、字型回退與效能尚未做實機驗證。工作區原有其他 V9 修改與未追蹤檔案均保留，未清除或覆蓋。
- 下一個最安全任務：先由使用者檢查首頁、事件與養成的視覺方向；確認後再把同一套層級語言延伸到合約與退休頁，並做 390／430px 實機 QA。未取得明確授權前不要上傳。

## V9.0 P0-P3 生涯控制台 UI 重製

- 階段：alpha／本機預覽；工作分支 `codex/v90-seed-progression`，未上傳、未合併、未部署。本節取代前一版「生涯轉播 UI」視覺方向。
- 首頁：移除失敗的轉播殼與重複手機覆寫，改為單一球場身份區＋建立球員區。手機首屏可直接看到姓名、位置與「踏上球場」；世界 Seed 收進可展開區，完整自訂仍保留身材、身份、外觀與出生地流程。
- 生涯頂部：手機只保留球員身份與 OVR，年齡、賽季與階段由當前章節承接；桌面保留完整球員列。現在進度改成單一摘要，事件決策成為主要內容。
- 事件與結果：一般三選一在 390px 同一視窗可完整比較，標題、代價與 12px 成功率皆保留；事件結果維持單一結果卡與橘色下一步按鈕，不再堆疊多層外框。
- 養成：天賦、訓練點數、可展開能力細節與推薦分配改為同一控制台。修正「每顆提供同等點數」的錯誤說明；能力點頁移除基礎成本等計算內語，改用一般培養／突破培養的玩家說法。舊存檔中的舊訓練摘要會只在顯示時轉成新文字，不重骰、不改能力。
- 跨尺寸修正：390、430、720、1440px 均確認無水平溢出。修正 720px 兩欄最小寬度與社群卡 content-box 溢位；修正桌面被舊 `max-width:720px` 裁掉位置、開始、自訂與 Seed 控制的 P0 問題。
- 實機流程：全新本機來源走過快速建立、球員誕生、季初特訓、推薦分配、一般事件、事件結果；另驗證完整自訂開啟、套用並返回。390px 一般事件三張選項皆在 844px 視窗內，主要按鈕至少 44px。
- 自動驗證：相關五個 JavaScript `node --check`、68/68 tests、`git diff --check` 通過。完整性測試已改為檢查實際生效的 `css/v9-shell.css`，未保留舊 CSS 只為讓測試通過。
- 範圍：沒有變更 Seed 數值、事件機率、能力計算、存檔欄位、排行榜、退休、D1、README 或正式 `main`。本機 Online API 404 為靜態 HTTP 預覽預期警告。
- 下一個最安全任務：由使用者先看 `http://127.0.0.1:43123/?v=v9-console-p0p3-final`；確認方向後再決定是否把同一層級延伸到合約、排行榜與退休頁。未取得明確授權前不要上傳。

## V9.0 事件舞台 UI 垂直切片

- 階段：alpha／本機預覽；工作分支 `codex/v90-seed-progression`，未上傳、未合併、未部署。本節取代前一版事件頁只靠外層 CSS 換皮的方向。
- 結構：一般事件進入時會把既有章節、標題、內文、結果、選項與下一步節點實際重組為「事件舞台＋決策面板」；離開事件時立即還原共用節點順序，因此合約、退休與其他共用畫面不受影響。
- 選擇：三種打法改為專屬決策卡，直接顯示高波動／攻守平衡／保留餘裕與成功機會；事件機率、效果類型、Seed 與結算演算未變更。
- 結果：結算後同一舞台切換成結果構圖，保留事件上下文，集中顯示臨場表現、目標、數值變化、稱號與下一步。
- 相容性：存檔仍只保存原本 `chapter`、`title`、`text`、`special`、`choices`、`flow` 與下一步按鈕內容，沒有新增或改寫 persisted schema；舊事件存檔續接時會用現行事件流程重建新版舞台。
- 瀏覽器驗證：本機 HTTP 已實際走過健康→賽季結算→能力點→下一季特訓→一般事件→結果→下一事件；1440、720、430、390px 均無水平溢出，390×844 三張選項完整顯示且每張約 102px 高，結果頁下一步按鈕可見。瀏覽器 console 無錯誤。
- 範圍：未變更 Seed、事件機率、能力、存檔欄位、排行榜、公開生涯、退休、D1、README 或正式 `main`。
- 下一個最安全任務：由使用者先看新版普通事件與結果構圖；方向確認後，再用同一套舞台語言延伸關鍵戰與合約抉擇。未取得明確授權前不要上傳。

## V9.0 統一生涯 UI 全面重製

- 階段：alpha／本機 Preview 3；工作分支 `codex/v90-seed-progression`，未上傳、未合併、未部署。本節取代前述「P0-P3 生涯控制台」與「事件舞台垂直切片」兩個已否決方向。
- 根因：前版只在一般事件階段搬動共用 DOM，其餘流程仍回到舊卡片；桌面固定高度又讓歷程與目前畫面形成多層捲動。另因沿用 `preview2` 資產標記，瀏覽器曾把新 HTML 與舊 CSS 混合顯示。
- 新骨架：遊戲內改為左側球員生涯欄＋右側階段工作區；手機改成緊湊球員列＋階段列。取消 V9 共用殼的固定視窗高度，所有階段回到正常頁面捲動。
- 差異化版型：生涯轉場是跨欄封面、特訓是左右養成工作台、一般事件是情境＋決策牌組、特殊事件是焦點舞台、健康是醫療報告、賽季結果是數據看板、能力點是成長面板、生涯抉擇是球團市場板。移除 `v9-event-stage.css` 與事件專用 DOM 搬移函式。
- 首頁：桌面改成大幅生涯封面＋窄版球員登錄單；390／430px 首屏依序顯示本週 Seed、品牌主張、姓名、位置與 60px 開始按鈕。完整自訂、舊存檔繼續與社群入口維持原功能。
- 相容範圍：沒有改動 Seed、能力與事件演算、存檔 schema、排行榜、公開生涯、退休資料、D1 或 README；只重整 `index.html` 的顯示骨架、`career-view.js` 的階段標籤與視窗配置，以及 V9 最後一層 CSS。
- 瀏覽器驗證：本機 HTTP 走過高中轉場→季初特訓→推薦分配→兩次一般事件→關鍵戰→健康→賽季結果→能力點→高中畢業市場→UBA 新篇章→下一季特訓→一般事件。桌面 1280、720、430、390px 均無水平溢出；390px 事件三張選項各 116px，推薦分配 44px，下一步 56px。
- 自動驗證：69/69 tests、`node --check js/ui/career-view.js`、`node --check js/events/event-engine.js`、`git diff --check` 通過。
- 目前風險：尚未用真實長生涯逐頁視覺檢查 NBA 自由市場、完整退休頁與公開生涯頁；這些資料與邏輯未被修改。正式 `main` 與正式站皆未變更。
- 下一個最安全任務：先由使用者檢查 `http://127.0.0.1:43123/?v=v9-unified-shell3` 的首頁、轉場、養成與事件方向；確認後再補做職業長生涯的合約／退休視覺 QA。未取得明確授權前不要上傳。

## V9.0 統一生涯 UI 撤回

- 結果：使用者否決「左側球員欄＋右側工作區」方向；已完整移除 `v9UnifiedShell`、階段專用選項結構與 `v9-shell.css`，恢復原本穩定的遊戲頁 DOM、事件卡、進度列與正常頁面捲動。
- 保留：V9 七級 Seed、天賦養成、玩家文字清理、存檔相容、排行榜分流與相關核心邏輯均未撤回或重算。
- 相容性：未變更存檔 schema、事件機率、關鍵戰、合約、退休、公開生涯、排行榜資料或 D1；正式 `main` 與正式站未變更。
- 本機預覽：`http://127.0.0.1:43123/?v=v9-ui-rollback1`。
- 下一個最安全任務：先停下整體 UI 重製；若之後重新設計，應先以獨立靜態 mockup 確認手機首屏與視覺方向，再接入遊戲 DOM，避免再次把未確認的殼直接疊入正式流程。

## V9.0 正式首頁精確恢復

- 更正：前一節只撤除了最後一版統一生涯殼，沒有恢復更早遭刪除的正式首頁 CSS 與 quick-start 安裝流程，因此「穩定版已恢復」的結論不成立。
- 根因：`css/growth-preview.css` 缺少 `main` 的正式首頁與手機規則；`js/ui/growth-preview.js` 停用了 `installQuickStartLayout()`，且函式內仍是後續實驗結構；沿用 `preview3` 又造成瀏覽器混載舊資產。
- 修正：以 GitHub 最新 `main@7729772` 原樣恢復 `css/growth-preview.css`、首頁 quick-start 結構與呼叫點；資產標記統一更新為 `9.0.0-preview4`。V9 Seed、專精養成、存檔相容與排行榜分流未撤回。
- 證據：正式站與本機均顯示左側生涯封面＋右側快速建角；必要 DOM 一致。390、430、720、1065px 無水平溢出，自訂球員開啟與返回正常；69/69 tests、相關 JavaScript `node --check`、`git diff --check` 通過。
- 本機預覽：`http://127.0.0.1:43123/?v=v9-home-main-restored2`。未上傳、未合併、未部署，正式 `main` 未變更。

## V9.0 隔離式 UI 方向原型

- 階段：prototype；沒有接入正式遊戲 DOM、存檔、Seed、排行榜或 API，只新增 `prototypes/v9-ui-concept/` 三個獨立檔案。
- 視覺方向：球員通道／球場燈光作主視覺，首頁採沉浸式封面＋浮接建角面板；生涯頁以「下一場關鍵戰」作唯一主焦點；關鍵戰頁獨立呈現三種打法與三項結果機率；V9 特訓頁以天賦型態、訓練點數與四項養成方向建立一致層級。
- 可操作流程：選擇位置→開始生涯→關鍵戰三選一→確認打法；生涯底部「能力」另行進入特訓→推薦分配。關鍵戰與特訓不再共用錯誤入口。
- UI 驗證：360、390、430、720、1080、1280px 的首頁／生涯／關鍵戰／特訓均無水平溢出；手機主要按鈕 60px、返回與推薦分配至少 44px、關鍵戰選項至少 129px。390×844 首屏可看到建角欄與開始按鈕，關鍵說明 13–14px、主要操作 16px。
- 自動驗證：`node --check prototypes/v9-ui-concept/app.js`、69/69 tests、`git diff --check` 通過。
- 本機預覽：`http://127.0.0.1:43123/prototypes/v9-ui-concept/?v=concept3`。未上傳、未合併、未部署；正式首頁與正式遊戲 UI 未修改。

## V9.0 核准 UI 正式接入預覽（文字遮擋修正版）

- 階段：production slice／本機 Preview 7；工作分支 `codex/v90-seed-progression`，未上傳、未合併、未部署。以使用者核准的 `concept3` 為唯一視覺方向，接入現有遊戲節點，不再建立第二套假遊戲殼。
- 首頁：正式 quick-start 流程改為球員通道主視覺＋浮接建角面板；新增 V9 專用 `assets/images/v9-arena-tunnel-hero.png`，不再借用退休分享背景。左上橢圓徽章已改成正常 `BasketballLife` 文字品牌列。姓名、位置、快速開始、完整自訂、世界 Seed 與舊存檔續玩都沿用原功能。
- 生涯：既有球員資料、階段列與目前事件節點保留；加入可實際操作的底部導覽。生涯回到當前事件，能力開啟即時八能力／天賦抽屜，紀錄開啟原生涯紀錄，排行呼叫既有排行榜流程。
- 養成：季初特訓改成天賦卡＋養成控制台；核心／延伸適性與骰點先顯示，推薦分配與八能力按鈕仍使用原事件函式。儲存後重建不重骰、不重算，完成後仍回到原本本季事件流程。
- 事件文字 P0：一般事件曾繼承舊版 `.choice:before` 左側編號欄與 `.choice:after` 箭頭，整個文字區被絕對定位元素吃掉。現在所有一般事件完全停用舊偽元素並使用完整卡片寬度；關鍵戰另以限定範圍的靜態編號欄呈現，不再共用舊定位。標題行高也已放寬，避免中文上下緣裁切。
- 關鍵戰：三種打法改成獨立決策卡，並列顯示球隊勝率、代表作機率與關鍵貢獻以上機率；選擇與結算仍呼叫原 `resolveSeasonKeyBattle`／國家隊流程。關鍵戰舞台也改用 V9 專用球員通道背景。
- 快取：所有本機 V9 資產統一使用 `9.0.0-preview7`，避免前幾版 HTML／CSS 混載。
- 瀏覽器驗證：以長球員名稱實際跑過兩個賽季，包含季初特訓、八個一般事件與結果、兩次關鍵戰、健康、年度成績、能力點與下一季。360、390、430、720×1280、1080×2400、1280px 均無水平溢出；一般事件三張卡的標題與說明 `clientHeight === scrollHeight`，舊左側欄為 `display:none`，每張可點擊並能一路推進。關鍵戰保留獨立 `01–03` 編號，選擇後下一步按鈕命中正常。
- 自動驗證：四個相關 JavaScript `node --check`、69/69 tests、`git diff --check` 通過。測試涵蓋 V8.1 舊存檔不套用 V9 天賦、V9／舊排行榜時代分流、七級 Seed、合約老將規則與核准 UI 使用正式存檔欄位。
- 風險：本機靜態 HTTP 無正式 Pages Functions，因此排行榜只驗證既有入口、錯誤狀態與版面，沒有寫入正式 D1。NBA 長生涯合約、完整退休與公開生涯仍需後續用長生涯資料做視覺驗收，相關邏輯本次未因 UI 接入而改寫。
- 本機預覽：`http://127.0.0.1:43123/?v=v9-approved-ui2`。目前工作區原有 `audit_v750.js`、`basketballlife-icon.svg` 與 `.wrangler/` 均未清除、還原或納入本次處理。

## V9.0 玩家流程 UI 補齊（Preview 9）

- 階段：本機 Preview 9；工作分支 `codex/v90-seed-progression`，未上傳、未合併、未部署。
- 進度：特訓與能力點的進度線改為反映目前能力，並標示培養上限；能力、成本、骰點與 Seed 演算未變更。
- 事件：一般事件恢復清楚的 `01／02／03`；關鍵戰選擇後切換成完整結果版型，下一步自動取得焦點並留在可操作視窗內。
- 流程：能力點、升學／新人市場、新賽季與排行榜完成 V9 介面層級；不再只有首頁、特訓與關鍵戰換版。
- 相容：舊存檔重建出的能力點 DOM 會補上 8 條新版進度線；沒有變更存檔 schema、退休資料、公開生涯或排行榜資料格式。
- 排行榜：只調整玩家介面與響應式錯誤狀態；完整全球排名、API、D1、送出與時代分流演算未改，也沒有連線正式資料庫做測試寫入。
- 快取：`index.html` 的 V9 CSS、JavaScript、資料與 manifest query 統一為 `9.0.0-preview9`。
- 瀏覽器 QA：實際走過特訓升級、一般事件、關鍵戰選擇／結果、能力點、升學市場、新賽季與排行榜入口；390、430、720×1280、1080×2400 均無水平溢出。
- 尚未發布：正式 `main`、Cloudflare、README 與正式版本號均未修改。工作區既有 `audit_v750.js`、`basketballlife-icon.svg`、`.wrangler/` 未清除、還原或納入處理。
- 下一個最安全任務：使用者先檢查 Preview 9 的特訓、一般事件、關鍵戰結果、能力點、升學市場、新賽季及排行榜版面；確認 UI 後，再安排正式 Pages Functions 環境的唯讀排行榜資料驗收。未取得明確授權前不要上傳。

## V9.0 球季結算與健康報告重製（Preview 10）

- 階段：本機 Preview 10；工作分支 `codex/v90-seed-progression`，未上傳、未合併、未部署。
- 問題修正：前版年度結算與健康頁仍使用舊卡片 DOM，只套 V9 顏色；背景球場線條不合理，且缺少清楚的籃框與籃球。這次不再靠外層換色，改寫兩頁的實際資訊結構與響應式配置。
- 年度結算：新增球季主視覺與成績分數、四項核心指標、賽事旅程、關鍵戰／傷病／獎項摘要、數據表與成長總結；既有比賽、獎項、關鍵戰與能力點資料來源及演算不變。
- 健康報告：新增健康主視覺、圓形身體狀態指標、受傷風險帶、醫療評估板、負荷與傷病歷史；健康與受傷兩條顯示路徑都使用新結構，傷病／疲勞／風險演算不變。
- 視覺資產：新增 `assets/images/v9-court-hero-v2.png`。畫面明確包含完整籃板、籃框、籃網與前景籃球；僅保留可辨識的直線邊界，不再使用錯誤球場弧線。舊圖未刪除。
- 導覽：820px 以上改為內容右側的直式生涯導覽，避免壓住年度報告；819px 以下維持底部導覽。下一步按鈕與導覽區域不重疊。
- 響應式 QA：390、430、720×1280、1080×2400、1280px 均確認無水平溢出；手機主視覺同時看得到籃框與籃球，健康／年度內容正常改為單欄，桌面年度主資料維持雙欄。
- 自動驗證：`node --check js/career/season-engine.js`、`node --check js/ui/growth-preview.js`、69/69 tests、`git diff --check` 通過。
- 相容範圍：未修改存檔 schema、Seed、關鍵戰機率、健康／傷病機率、比賽結果、排行榜 API／D1、退休、README、正式版本號或正式 `main`。
- 本機預覽：`http://127.0.0.1:43127/?v=v9-preview10-final`。下一個最安全任務是先由使用者驗收年度結算與健康報告，再決定是否以同一層級重製其他尚未核准的畫面；未取得明確授權前不要上傳。

## V9.0 跨畫面排版修正（Preview 11）

- 階段：本機 Preview 11；工作分支 `codex/v90-seed-progression`，未上傳、未合併、未部署。
- 養成：八能力詳細資料改為適合左側天賦欄的兩欄卡片，移除舊版卡片內固定三欄與能力數值最小寬度；能力值、進度條與成本文字不再被裁切。
- 關鍵戰：清除舊版成功率 pill 誤套到整個機率區塊的外框，三項機率與疲勞／負荷提示分開排版，提示文字不再被線穿過。
- 新賽季：沿用已檢查完整的 `v9-court-hero-v2.png`，改正偽元素只佔右半邊再 `cover` 的裁切方式；手機改以完整寬度顯示，籃框、籃網與籃球均保留。沒有重新生成圖片。
- 一般事件：成功率改為獨立資訊卡，顯示「成功機會」、百分比與短進度條；事件順序、機率計算、選擇與結算邏輯不變。
- 響應式 QA：390、430、720、1080、1280px 均無頁面或內容水平溢出；390px 一般事件與關鍵戰提示沒有文字裁切，八能力卡桌面各區塊溢位為 0。
- 自動驗證：`node --check js/events/event-engine.js`、69/69 tests、`git diff --check` 通過。
- 相容範圍：未修改 Seed、能力／事件機率、存檔 schema、排行榜 API／D1、退休、README、正式版本號或正式 `main`。下一個最安全任務是由使用者檢查 Preview 11 四個修正版面；未取得明確授權前不要上傳。

## V9.0 生涯殿堂原型與開季規劃修正（Preview 12）

- 階段：排行榜／退休頁為隔離 prototype；開季規劃為正式 V9 顯示層修正。工作分支 `codex/v90-seed-progression`，未上傳、未合併、未部署。
- 排行榜原型：新增 `prototypes/v9-legacy-hall/`，以「榜首焦點、我的完整全球名次、前 50 榜單、版本紀錄室」建立新資訊層級；本人名次仍以既有 API 的 `global_rank` 概念呈現，原型不呼叫 API、不寫入 D1。
- 退休原型：以歷史定位、核心數字、生涯定論、代表篇章、選擇回聲、完整數據與公開狀態分頁呈現；示例資料不會寫入存檔，也沒有修改退休演算、名人堂或公開生涯流程。
- 開季規劃：三個策略選項改為同寬決策卡；策略標籤與季傷風險拆成右側資訊塊，390／430px 改成卡片內完整風險列。傷病機率、方案效果、事件順序與存檔欄位均未改。
- 本機預覽：生涯殿堂 `http://127.0.0.1:43127/prototypes/v9-legacy-hall/?v=2`；開季規劃 `http://127.0.0.1:43127/prototypes/v9-legacy-hall/season-plan.html?v=6`。
- 瀏覽器 QA：排行榜與退休頁在 390、430、720、1080、1280px 無水平溢出；四種排名指標可切換，退休四個分頁均切到正確內容。開季規劃三張卡在桌機同寬 1031px，430px 同寬 359px，390px 同寬 319px；可見按鈕高度皆至少 44px。
- 自動驗證：`node --check prototypes/v9-legacy-hall/app.js`、`node --check js/career/injury-engine.js`、69/69 tests、`git diff --check` 通過。
- 相容範圍：未修改排行榜 API／D1、BL POWER、存檔 schema、退休資料、公開生涯送出、README、正式版本號或正式 `main`。既有 `audit_v750.js`、`basketballlife-icon.svg`、`.wrangler/` 未清除或還原。

## V9.0 生涯殿堂完整內容與互動原型（Preview 13）

- 階段：排行榜／退休頁與開季規劃皆為本機可操作預覽；未接入正式退休資料、公開生涯 API 或存檔寫入，未上傳、未合併、未部署。
- 退休頁：保留核准的球場主視覺與生涯定論，分成「生涯總覽、殿堂與榮譽、生涯故事、完整數據」四區；補回 NBA／國家隊名人堂票數、球衣退休、主要榮譽、聯盟與國家隊摘要、場外生涯、傷病復出、Seed 與 23 季逐季資料。
- 分享：新增引退故事圖預覽，只保留球員身分、BL POWER、完整全球排名、名人堂、球衣退休、冠軍與一段生涯轉折；完整票選與逐季資料留在公開頁。刪除正式接入、API 概念與示例資料等玩家不需要的說明。
- 開季規劃：隔離預覽加入真實可點擊流程；三種策略可選、`aria-pressed` 狀態互斥、確認按鈕在選擇前停用，確認後顯示本季影響。正式 `chooseSeasonPlan()` 與傷病機率演算沒有修改。
- 瀏覽器 QA：1280px 實際檢查桌面退休首屏；390px 實際檢查退休首屏與開季規劃。390、430、720、1080px 均無水平溢出；退休四個分頁可切換、分享視窗可開關、完整生涯入口可到 23 季表格；三種策略皆可選擇與確認。可見按鈕至少 44px。
- 自動驗證：`node --check prototypes/v9-legacy-hall/app.js`、`node --check prototypes/v9-legacy-hall/season-plan.js`、`node --check js/career/injury-engine.js`、70/70 tests、`git diff --check` 通過。
- 相容範圍：未修改排行榜 API／D1、BL POWER、名人堂／球衣退休演算、退休存檔、公開生涯送出、README、正式版本號或正式 `main`。下一步應由使用者先驗收兩個本機預覽，再決定是否將退休原型接入正式資料 owner。

## V9.0 SSS+ 完整退休生涯案例（Preview 14）

- 階段：隔離 prototype；只更新 `prototypes/v9-legacy-hall/` 的展示資料、版面與回歸斷言，未接入正式退休演算、存檔、排行榜 API 或 D1。
- 案例：林承遠，SSS+ 神話／全能串聯者，23 季、1,474 場、BL POWER 18,942。巔峰八項能力總和 775、OVR 97，終結／傳球／球商三項 99，符合 SSS+ 的 3–4 項頂尖專長與總上限預算，不是八項全滿。
- 內容：新增事實型歷史定位、五冠王朝、四次 MVP／FMVP、阿基里斯腱重傷後轉型、返鄉最後一舞、雙首輪名人堂、球衣退休、同時代人物、三段選擇回聲、完整能力與 23 季逐年數據；分享預覽同步改用同一組證據。
- 操作：`?v=4&career=sssplus` 直接開啟退休頁；排行榜仍可返回。修正分享視窗開啟後焦點落在背景遮罩的問題，現在會聚焦右上角關閉按鈕。
- 瀏覽器 QA：桌面首屏、SSS+ 能力區、四個退休分頁、23 季表格與分享視窗實際操作通過；390／430px 無頁面水平溢出，按鈕最低 44px，能力卡與分享視窗無裁切，退休分頁保留橫向滑動但隱藏原生捲軸。
- 自動驗證：`node --check prototypes/v9-legacy-hall/app.js`、V9 針對性 9/9、完整 70/70 tests、`git diff --check` 通過。
- 相容範圍：未修改正式 V8.1.1 退休頁、名人堂／球衣退休／BL POWER 演算、舊存檔、公開生涯、README、版本號或正式 `main`；未上傳、未合併、未部署。

## V9.0 SSS+ 退休頁資料可信度修正（Preview 15）

- 階段：隔離 prototype；本節取代 Preview 14 中自行設定的排行榜、BL POWER 與分享圖內容。未上傳、未合併、未部署。
- 根因：Preview 14 把靜態案例的 `#1`、`18,942 BL POWER` 與自製分享圖做得像正式結果，但這些數字沒有經由現行排行榜 API 或 `careerPowerModel` 產生，會讓人誤以為演算法已被修改。
- 修正：移除原型排行榜、假完整全球名次、假 BL POWER 與自製退休分享圖；正式排行榜、BL POWER 演算與 V8.1.1 分享圖完全未改。退休頁只保留能由案例生涯資料支持的 23 季、逐季成績、主要獎項、名人堂票數、球衣退休、傷後轉型、同時代人物與 SSS+ 八能力。
- 資料一致：案例改回 23 季、1,315 場、27,707 分、三座 NBA 冠軍、巔峰 OVR 96；SSS+ 八項總和 768，終結／傳球／球商三項 99。四個退休分頁與 23 季表格可操作。
- 版面：退休首屏縮為 480px 桌面／555px 手機，移除大面積空白；公開區縮為兩個操作，不再用三個滿版按鈕。文案改用冠軍、膝傷轉型與返鄉三段具體事實，不再使用無法驗證的泛用生涯定論。
- 瀏覽器 QA：1280、430、390px 無水平溢出或文字裁切；四個退休分頁均能切換，所有可見按鈕至少 46px，23 季資料列完整，瀏覽器沒有錯誤或警告。
- 自動驗證：`node --check prototypes/v9-legacy-hall/app.js`、70/70 tests、`git diff --check` 通過。
- 本機預覽：`http://127.0.0.1:43127/prototypes/v9-legacy-hall/?v=5&career=sssplus`。正式 `main`、README、版本號、排行榜 API／D1、BL POWER、退休演算、公開生涯與正式分享圖均未修改。

## V9.0 退休分享圖相容性恢復（Preview 17）

- 階段：本機隔離 prototype；未上傳、未合併、未部署。本節取代 Preview 16 曾加入的兩套自製 Canvas 圖片。
- 修正：完整移除原型的 `buildRetirementStoryImage()`、`buildCareerPoster()`、PNG Blob、下載／複製與自製圖片預覽視窗；原型不再維護第二套退休圖片版型。
- 正式圖片：`generateRetirementPageImage()`（引退故事圖）與 `generateCareerImage()`（完整生涯紀錄長圖）沿用 V8.1.1 正式實作。比對最新 `main` 後，另將完整長圖頁尾曾被改成動態 V9 版本字樣的一行恢復為原本 `BasketballLife · V8.1`；兩個正式產圖函式與圖片版型相對 `main` 無差異。
- 保留內容：退休頁仍顯示現行 `careerPowerModel` 算出的 BL POWER、六則球迷回聲、名人堂／球衣退休、23 季完整數據、傷病復出、場外生涯與 Seed 資料。
- 原型入口：兩個按鈕保留正式名稱，整合正式退休資料時只呼叫既有產圖函式；隔離原型沒有正式退休玩家狀態，因此不偽造圖片，點擊會提示到已退休生涯製作。
- 驗證：`node --check prototypes/v9-legacy-hall/app.js`、`node --check js/ui/retirement-view.js`、70/70 tests、`git diff --check` 通過。452px 本機瀏覽器確認 BL POWER、6 張球迷回聲、兩個產圖入口正常，頁面無水平溢出且不存在自製圖片預覽節點。
- 本機預覽：`http://127.0.0.1:43127/prototypes/v9-legacy-hall/?v=7&career=sssplus`。`main`、README、正式版本號、排行榜 API／D1、退休演算與正式分享圖片內容均未發布或改版。

## V9.0 核准退休頁正式資料接入（Preview 18）

- 階段：本機 production slice；工作分支 `codex/v90-seed-progression`，未上傳、未合併、未部署。核准的 Preview 17 退休頁版型已接入正式退休流程，不再把新資料塞回舊 `legacyDashboard` 外殼。
- 正式退休頁：V9 已退休生涯改為球場主視覺、BL POWER、生涯事實定位、五項核心數據與「生涯總覽／榮譽殿堂／生涯故事／完整數據」四頁籤；舊遊戲標頭、左側生涯軌道、固定視窗高度與底部生涯導覽在退休模式停用。V8.1 舊生涯仍沿用原退休頁，未被套用 V9 版型。
- 資料來源：歷史定位、代表球季、場上角色轉型、重要選擇、同時代人物、名人堂得票率、球衣退休、獎項、國家隊、逐季數據、傷病、收入與 Seed／八項養成上限均來自正式玩家存檔；沒有使用原型寫死的 G6、排名或 BL POWER 數字。
- 保留功能：現行 `careerPowerModel`、名人堂／球衣退休判定、公開生涯與完整全球名次流程均未改；球迷回聲完整保留。`generateRetirementPageImage()` 與 `generateCareerImage()` 仍是原本兩個正式產圖函式，Canvas 內容未重做。
- 相容性：新增 V9 顯示模式 class 與動態 HTML builder；舊存檔若沒有 `roleHistory`，仍從逐季上場時間與產量推導角色時間軸。沒有移除或重新解讀既有退休、排行榜或公開生涯欄位。
- 介面收尾：退休分享區解除舊版 `100%` Flex 與雙欄 Grid 衝突，桌面兩個正式產圖按鈕維持同列，區塊實測高度由異常長版收至 87px；手機改為單欄按鈕，無水平溢出。生涯頂部維持原分隔式資訊列，生涯階段移到 OVR 前方；OVR 取消圓框與光暈，標籤與數值改為金色，OVR 演算未改。
- 瀏覽器 QA：隔離 HTTP `http://127.0.0.1:43128/?v=v9-retirement-factual1` 已載入 SSS+ 退休生涯。桌面確認舊生涯軌道為 0、6 則球迷回聲與兩個原產圖 handler 存在；390／430px 的四個頁籤均可切換，頁面寬度未超出 viewport，未偵測到可見文字裁切，舊標頭與舊動態紀錄均為 `display:none`。
- 自動驗證：`node --check js/ui/retirement-view.js`、`node --check js/ui/career-view.js`、完整 73/73 tests、`git diff --check` 通過。
- 下一個最安全任務：由使用者直接檢查 `43128` 的總覽、榮譽、故事與完整數據；確認內容密度後再清除隔離 QA 檔並做發布前總差異審查。未取得明確授權前不要上傳、合併、部署或修改正式 D1。

## V9.0 頂部重新開始選單修正

- 根因：V9 將重新開始選單移入頂部列後，沿用舊 `.header{overflow:hidden}`，使選單與球員資訊重疊且下半部被裁切。
- 修正：V9 頂部列允許選單溢出，桌面選單定位到完整標頭下方；手機改為標頭同寬操作面板，重新開始按鈕降低視覺重量，兩個選項提高文字與觸控高度。
- 驗證：桌面、390px、430px 均完整顯示兩個選項，與標頭間保留約 5–7px，沒有水平溢出，按鈕中心命中正確且可正常關閉；73/73 tests、`git diff --check` 通過。
- 範圍：只修改 `css/v9-ui.css`；未修改 Seed、存檔、排行榜、版本號、README、正式 `main`，未上傳或部署。

## V9.0 七級生涯、D1／排行榜與跨裝置發布前 QA

- 階段：本機發布前驗證；工作分支 `codex/v90-seed-progression`，未上傳、未合併、未部署，也未寫入正式 D1。
- 七級生涯：以實際遊戲函式完成 SSS+、SS+、S+、S、A、B、C 各一段完整生涯。六段生涯推進至 50 歲退休，C 級案例在 22 歲未取得職業市場後結束；每季賽程、出賽、統計、合約／市場、傷病、獎項與退休狀態均通過結構檢查。單次樣本只驗證流程完整性，不作為級別強弱的單調性證明。
- D1／API：新增 `tests/v90-d1.test.mjs`，以實際 SQLite 語法套用 0001–0004 migration，確認 V7／V8／V8.1 舊列不變；V9 預設榜、前 50、完整 `global_rank`、每週榜分流、POST 驗證、公開生涯與版本冠軍路徑均通過。正式站僅以 GET 驗證 D1 健康與 V7／V8／V8.1／2026W35 分流，沒有建立測試生涯或寫入資料。
- UI QA：桌面、390px、430px 走過首頁、建角、手機標頭、季初特訓、能力進度、三個事件選項、事件結果、下一步定位、重新開始選單、退休四頁籤，以及兩個既有退休產圖流程；未觀察到新的文字裁切、水平溢出或卡死。環境沒有連接 Android／iPhone，因此這是瀏覽器響應式 QA，不冒充實體裝置測試。
- 資產：`index.html` 37 個本機 manifest／CSS／JS／data query 統一更新為 `9.0.0-preview19`；HTTP 重載均回應 200。正式 `careerVersion` 仍是 `9.0.0`，README 與 manifest 內容未改。
- 自動驗證：36 個 JavaScript 檔案 `node --check`、75/75 tests、`git diff --check` 通過。

## V9.0 玩家殿堂單一席次排行榜（Preview 20）

- 階段：本機 production slice；工作分支 `codex/v90-seed-progression`，未上傳、未合併、未部署，也未修改正式 D1 資料。
- 排名規則：一般榜、舊版本榜與每週榜的每個指標，改為每位 `user_id` 只保留該指標最佳公開生涯；不同指標可由不同生涯代表。排行榜前 50 現在是 50 位玩家，不是 50 筆可重複佔位的生涯。
- 資料保留：沒有刪除或改寫任何 `career_records`。API 仍回傳公開生涯總數，另加入 `public_career_count`、`player_career_rank`、唯一玩家 `global_rank` 與 `ranking_total`；本人查詢仍可取得全部公開生涯，只有代表生涯擁有正式名次。
- 退休相容：舊退休存檔每次確認公開生涯時都會重新取得唯一玩家名次；若該生涯已不是 BL POWER 或巔峰 OVR 的代表，舊名次會清空但公開生涯不會消失。排行榜總數後備值改用玩家數，不再誤用生涯筆數。
- UI：V9 榜單更名為「玩家殿堂」；全球榜顯示「代表生涯」與同玩家其餘公開生涯數；我的公開生涯區分「本項代表／其他生涯」，其他生涯以 `—` 顯示未佔正式席次。手機排行榜頁籤隱藏原生捲軸並維持 44px 觸控高度。
- 回歸：D1 SQLite fixture 以同玩家 9 段生涯驗證只佔一席、不同指標代表可切換、前 50 為 50 個唯一玩家、榜外本人完整名次、每週舊重複列防禦性去重、舊版本隔離與所有原始生涯計數保留。
- 本機預覽：`http://127.0.0.1:43131/?leaderboard=power&era=v9&v=preview20`，由 `.codex-leaderboard-qa.mjs` 提供隔離 SQLite 資料；該 QA helper 不應納入正式上傳。
- 驗證：桌面、390px、430px 實際 HTTP 檢查均無水平溢出或文字裁切；BL POWER／巔峰 OVR 切換會自動換成各玩家對應代表生涯。相關 `node --check`、75/75 tests、`git diff --check` 通過。`index.html` 37 個本機資產 query 統一更新為 `9.0.0-preview20`。

## V9.0 核准玩家殿堂正式接入（Preview 22）

- 階段：本機 production slice；工作分支 `codex/v90-seed-progression`，未上傳、未合併、未部署，也未寫入正式 D1。
- 介面：把先前核准的玩家殿堂設計正式接入排行榜流程，包含球場通道主視覺、V9／每週 Seed／版本紀錄雙層切換、前三名焦點台、我的完整全球名次、第 4–50 名清單與版本紀錄室。排行榜不再沿用舊版卡片清單只換顏色。
- 排名規則：延續 Preview 20 的單一席次 API；每位玩家在每個排行項目只由最佳公開生涯佔一席，不刪除其餘生涯。排行榜玩家數與公開生涯數分開顯示，本人仍可展開其他公開生涯。
- 榜別資訊：V9 現役榜右側只說明「最佳生涯代表／每項一人一席」；本週週次、位置與身高只在「每週 Seed」顯示；「版本紀錄」只顯示版本封存資訊。三個榜別不再共用錯誤的 Seed 卡。
- 快取修正：排行榜快取原本只還原全球列與統計，沒有同步還原 `myPublicCareerRows`，跨 BL POWER／巔峰 OVR／版本紀錄切換時可能殘留上一指標的本人名次。現在同一快取鍵會一起儲存與還原本人公開生涯列。
- UI QA：本機 HTTP `http://127.0.0.1:43131/?leaderboard=power&era=v9&v=preview22` 已檢查桌面、390px、430px；無水平溢出，排行切換控制約 44px，前三名在手機以第 1 名優先排列。同玩家 9 段生涯在全球榜只出現一次。
- 行為 QA：BL POWER 顯示本人完整名次 `#6`，巔峰 OVR 顯示 `#2`，進入版本紀錄再切回 BL POWER 後正確回到 `#6`；版本紀錄室與每週榜入口仍保留。
- 自動驗證：`node --check js/leaderboard/leaderboard-api.js`、`node --check functions/api/[[path]].js`、75/75 tests、`git diff --check` 通過。`index.html` 37 個本機資產 query 統一更新為 `9.0.0-preview22`；正式版本號與 README 未改。

## V9.0 選秀結果與舊 Preview 存檔重建（Preview 28）

- 選秀前：各市場顯示球探評價、預測入口、球隊位置需求與適配程度；NBA、台灣、CBA 與 G League 會產生輪次／順位或雙向、落選訓練營入口。
- 選秀後：正式結果與合約分開呈現，首輪、次輪、雙向與訓練營會影響年限、保障、球隊耐心、初始角色及保障金額。
- 舊 Preview 相容：繼續遊戲時若停在舊選秀結果頁，使用已保存的 `collegeDraftHistory` 重建新版結果；保留原成功／失敗、機率與擲骰，不重新抽選。實際舊存檔已由 CBA 泛用新人合約重建為首輪第 5 順位與對應保障。
- UI：國家隊關鍵戰補上正式 V9 key-battle 標記，三個選項改回單欄，避免桌面與手機卡片互相覆蓋。
- 驗證：實際 HTTP 舊存檔、桌面／430／390px 均無水平溢出；相關 JavaScript `node --check`、完整 83/83 tests、`git diff --check` 通過。37 個本機資產 query 統一為 `9.0.0-preview28`。
- 未完成但已確認：50 歲仍被 `canReceiveStandardContract()`、公開測試與合約選項以年齡絕對封鎖，和「年齡不直接抹除當下能力」不一致。此輪只做唯讀分析，退休算法未修改。
- 範圍：未上傳、未合併、未部署，未修改正式 D1、README 或正式版本號。

## V9.0 高齡續戰與退休市場修正

- 階段：本機 production slice；工作分支 `codex/v90-seed-progression`，未上傳、未合併、未部署，也未修改正式 D1。
- 退休線：移除 50 歲標準合約、NBA 續留、G League 正式門檻、公開測試、合約選項與返鄉最後一舞的絕對封鎖。有效合約與球員選項不再因跨過 50 歲失效。
- 續戰判定：新增老將續戰評估，綜合 OVR、體能、健康、耐久、身體負荷、出勤、場均時間、實戰產量、近期重大傷病及聯盟名單門檻；年齡不直接加入續戰分數。
- 合約風險：35 歲後年齡只作用於合約年限、薪資風險、保障比例與球隊耐心。合約卡會分別顯示合約總值、保障金額、保障比例、選項與球隊耐心，不再把選項文字誤當保障金額。
- 最後一舞：只在母隊、自由市場及公開測試都失敗後的退休危機頁提供；取消 50 歲專屬文案與上限，完成告別球季後正常推進一年再退休。
- 老化判定：沒有再加速既有老化。47 歲後原規則每季八項能力合計約下降 28 點，舊傷與高負荷還會再加重；先前問題是市場沒有正確使用老化後的能力與身體狀態，不是能力掉得太慢。
- 49–55 模擬：普通老將會隨能力、健康與出勤下降逐步失去市場，55 歲案例沒有標準合約；體能怪物在同齡仍保有符合能力的職業市場，但合約更短、保障與耐心更低。另驗證健康高產的 50／55 歲球員不會因年齡直接失去 NBA 資格。
- 驗證：`node --check js/career/contract-engine.js`、`node --check js/career/retirement-engine.js`、完整 87/87 tests 與 `git diff --check` 通過。正式版本號、README、排行榜資料與正式 `main` 均未修改。

## V9.0 精華生涯成長與例行選項補正

- 根因：精華模式原本只自動執行一般事件，特殊事件中的例行選項直接被濾掉；這會漏掉能力 +1／+2、特殊能力點與其他選項效果，使 OVR 成長偏慢。
- 修正：例行特殊事件現在依 Seed 自動選擇並呼叫原有 resolver，完整套用能力、`specialBonusPoints`、健康、疲勞、關係與其他既有效果；只保留關鍵戰、重大故事、國家隊、交易、手術、復出、經紀人與隊友等重大節點給玩家。
- 相容性：完整生涯流程不變；精華模式仍不增加每季手動選擇，重大節點仍會停下。自動選項與一般事件都寫入 `highlightHistory`，不新增取代既有能力欄位。
- 驗證：精華專項 5/5、完整 169/169 tests；變更 JS `node --check`；`git diff --check` 通過。尚未發布、未合併、未修改正式 D1。

## V9.0 原創場景事件與專屬結果文本

- 內容：一般事件池由 60 個增加為 68 個；新增 4 個高中／大學共用籃球場景與 4 個職業場景。每個新事件都有 3 個具體做法，且每個做法各自提供大成功、成功、失敗、大失敗文本，共 96 段專屬結果。
- 實作：一般事件選項可選擇性攜帶結果文本；舊事件沒有新欄位時仍使用原本結果邏輯，不改舊存檔與數值計算。
- 邏輯限制：客場飯店隱私事件只會出現在確實有長途客場住宿脈絡的海外職業聯賽；對手公開研究事件要求球隊評價至少 8；五犯離場制的記錄台錯帳事件不會出現在 CBA、NBA G League 或 NBA。曾與既有生涯故事重疊的贊助拍攝題材已移除並改為記錄台犯規錯帳。
- 驗證：事件專項 12/12、完整 172/172 tests、`git diff --check` 通過；本機 `http://127.0.0.1:4173/index.html?preview=show` 正常載入。尚未發布、未合併、未修改版本、README 或正式 D1。

## V9.0 事件文本第二批：階段／聯賽邏輯稽核

- 抽查：瀏覽器實際讀取一段 CBA 跨隊家庭特殊事件，人物、前情與球隊轉換一致；另以正式 `normalEventPool()` 對 HBL、五種大學與八種職業路徑逐一取池驗證。新建高中瀏覽器流程曾因分頁操作逾時中止，沒有把該項冒充為完成的端到端實玩。
- 淘汰空泛文本：重寫 12 個舊事件，移除「正常」「成長」「穩健」等 2～7 字說明，改成具體比賽問題、行動、代價與可能失去的角色。
- 新增內容：事件池由 68 增至 77；新增 9 個限定情境事件，涵蓋 HBL、大學、SBL、台灣職籃、日韓職籃、CBA、NBA G League、歐洲聯賽與 NBA。每個新事件有 3 個選項、每選項 4 種專屬結果，共新增 108 段結果文本。
- 邏輯修正：移除任何職業球季都可能抽到的「季後賽第一戰」，改為不預設晉級的焦點戰；航班延誤與飯店五天四戰不再出現在 SBL／台灣職籃；本土連續客場不再假設長途移動。
- 驗證：事件專項 15/15、完整 175/175 tests；新增路徑限定、旅外移動、季後賽前提、舊文本最低內容密度與專屬結果完整性測試；相關 JavaScript `node --check`、`git diff --check` 通過。本機預覽 `http://127.0.0.1:4173/index.html?preview=show&qa=event-text-77` 正常載入。未發布、未合併、未修改版本、README 或正式 D1。

## V9.0 事件文本第三批：職業短文本與條件式新場景

- 淘汰空泛文本：完整重寫「戰術角色調整、投籃低潮、影片會議、關鍵第四節、主場噓聲、隊友進入合約年」6 個職業事件。每題改成可辨認的球場或球隊前提、3 個不同策略與各自 4 級結果，共補齊 72 段專屬結果，不再落回泛用能力／信任句。
- 新增內容：職業事件池由 39 增至 44，總一般事件由 77 增至 82。新增「對手開始放空你的外線、換防後對手連續點名你、賽前發言輪到你、年輕隊友帶著影片來敲門、最後一個輪替名額」5 題，每題 3 個選項與 12 段專屬結果，共新增 60 段結果文本。
- 邏輯限制：外線與防守點名事件只對相應能力不高於 67 的球員開放；領袖發言要求 28 歲、至少第 8 個生涯球季且評價 10；指導年輕隊友要求 25 歲與至少第 6 季；最後輪替名額只在上一個確實存在的職業球季平均時間不高於 18 分鐘時出現。缺少能力或前季數據時不猜測、不開放事件。
- 相容性：只為事件資料加入可選條件讀取；舊事件、舊存檔、選項效果、數值平衡、完整／精華生涯排程均未改。
- 驗證：事件專項 17/17、完整 177/177 tests、相關 JavaScript `node --check`、`git diff --check` 通過。舊 `audit_v750.js` 因本機缺少 `jsdom` 未能執行，沒有把它列入通過項目。本機預覽 `http://127.0.0.1:4173/index.html?preview=show&qa=event-text-82` 回應 200。未發布、未合併、未修改版本、README 或正式 D1。

## V9.0 精華生涯 12 章排程骨架

- 節奏：精華生涯改以 12 章為標準目標，依高中 2、大學 2、職業前中期 5、老將期 3 配額選出可玩章節；高中直升職業路線因沒有大學階段，標準為 10 章。首頁標示約 20～30 分鐘、全程約 10～14 章。
- 排程：同一 Seed 的章節選擇保持確定性；排定章節在生涯故事與球季關鍵戰間交錯，其餘平淡球季仍沿用既有自動推進與原始數值公式，不另建能力、合約或老化模型。
- 重大事件：交易、國家隊、手術、術後復健、復出與最後一舞會強制停下；新發生的重大傷病與奪冠球季也會中止自動推進。強制事件會占用該階段章節配額，避免每多一件大事就無限增加手動選擇。
- 事件處理：未選為章節的例行特殊事件仍呼叫原 resolver 套用完整數值後果；未選中的長期人物故事延後，不替玩家自動作主。完整生涯的特殊事件佇列保持原樣。
- 存檔相容：新增 `highlightChapterHistory` 作為附加欄位；舊存檔缺少該欄位時正規化為空陣列，且未指定 `careerMode` 的舊存檔仍走完整生涯。
- 介面：可玩事件顯示「精華生涯・第 N 章・年份・聯盟路徑」；首頁與模式說明同步為約 20～30 分鐘、10～14 章。本機預覽已開在 `http://127.0.0.1:4173/index.html?preview=show&qa=highlight-12`。
- 驗證：精華專項 9/9、完整 181/181 tests、5 個相關 JavaScript `node --check`、`git diff --check` 通過；桌面瀏覽器確認精華模式可選、開始按鈕與說明正確，瀏覽器錯誤紀錄為空。
- 尚未冒充完成：20～30 分鐘目前是依章節預算設定的設計目標，尚未做真人計時的完整生涯實玩；畢業、選秀與合約等獨立決策頁仍是強制停點，但目前沒有沿用事件頁的章節編號。下一個最安全任務是做一輪計時實玩，記錄實際停點與總時長，再只針對超時處調整配額或轉場。
- 範圍：未上傳、未合併、未部署，未修改正式版本號、README、正式 D1 或正式 `main`。

## V9.0 精華生涯主線接回與 OVR 成長修正

- 主線根因：精華模式的自動季前流程完成訓練與一般事件後直接進入特殊事件，繞過完整模式使用的 `maybeStartV90MidcareerRhythm()`；因此「巔峰延續／角色轉折／生涯收束」程式與測試雖然存在，精華生涯實際不會正常進入。
- 主線修正：精華季前自動結算後會先檢查正式三段主線。三段主線仍依實際 OVR 曲線、巔峰落差、健康、身體負荷、角色與市場狀態觸發，不以固定年齡硬塞；每段內的 2／2／3 個節點共用一個精華章號並顯示段內進度，不把 7 次主線抉擇膨脹成 7 個額外章節。
- OVR 根因：精華季末自動能力點錯用 `availableTrainingGrowth()` 判斷手動能力點，而且依位置順序購買；已完成季初訓練的能力可能被錯誤排除，能力點也可能先買昂貴升級，導致同樣點數換到較少永久能力，OVR 確實可能比完整模式玩家手動分配慢。
- OVR 修正：自動能力點改用正式 `availableManualGrowth()` 額度，並在所有合法能力中優先購買成本最低的永久提升；一般事件也不再三選一等機率亂選，而是優先選擇預期永久成長較高的方案，身體高負荷時才提高檢查、休息與限時上場權重。所有結果仍使用原 `eventChance()`、`resolveEvent()`、`pointCost()`、`buyPoint()`、Seed、年齡與每季成長上限，不額外贈送能力、不改完整模式數值。
- 驗證：精華專項 13/13、三段主線 9/9、完整 185/185 tests；三個修改 JavaScript `node --check` 與 `git diff --check` 通過。專項案例確認 4 點能力可換成 4 次合法 OVR 提升，而不是被訓練額度或昂貴位置優先順序卡住；一般事件在健康狀態優先成長，在高疲勞／高負荷狀態改選恢復方案。
- 瀏覽器：本機 `http://127.0.0.1:4173/index.html?preview=show&qa=highlight-midcareer-ovr` 載入無 console error，精華模式、20～30 分鐘說明與開始入口可見；因瀏覽器已有使用者退休存檔，本輪沒有覆蓋該存檔來冒充全新端到端實玩。
- 範圍：只接回既有主線與修正精華自動分配；完整生涯、主線判定公式、Seed、合約、老化、排行榜、正式版本號、README、正式 D1 與正式 `main` 均未修改，也未上傳、合併或部署。

## V9.0 每輪限定長期主線

- 抽取規則：每段新生涯只開放 5 條長期主線，固定為 2 條人物線、1 條球隊／教練線、1 條人生線、1 條生涯後期線；其餘長期主線本輪鎖定。60 個階段限定單事件不受影響，仍依真實生涯條件進入事件池。
- 路線時機：高中階段先決定人物線；進入大學或職業後，才用當時 `path` 決定球隊／教練線與人生線；進入真正老將階段後再決定後期線。同一 Seed 與同一路線結果固定，不同 Seed 或升學路線可形成不同組合。
- 分組：人物線為宿敵、朋友、隊友事件；球隊／教練線為教練角色、合約市場、重建核心；人生線為傷病改造、家庭城市、媒體身分、國家隊落選；後期線為新秀導師、生涯最後一章。
- 舊存檔相容：新增附加欄位 `careerStoryLineSelection`。舊存檔缺少欄位時自動建立；所有已出現、已排程或已記錄的長期主線都會納入開放清單，即使超過新配額也不會中斷既有故事。
- 驗證：故事專項 40/40、完整 189/189 tests、4 個修改 JavaScript `node --check`、`git diff --check` 通過。測試涵蓋 2＋1＋1＋1 配額、同 Seed 確定性、不同 Seed／路線差異、未選長線不進池、階段事件仍保留，以及舊存檔已開始主線不遺失。
- 範圍：未新增第四節點，未修改三段中生涯主線、精華 OVR、合約、老化、排行榜、正式版本號、README、正式 D1 或正式 `main`；未上傳、未合併、未部署。
- 下一步：圖片中的第四階段缺口仍成立，結果頁仍會提前顯示下一章名稱與預定年份。下一個最安全任務是只移除這段未保證預告，改成顯示已發生的當下後果與已留下的關係狀態，不改事件排程本身。

## V9.0 長期故事第四節與 ONLINE 關鍵戰原型

- 長期故事：事件庫由 96 增至 104 個節點、312 個選項；宿敵、朋友、教練角色、傷病改造、隊友風波、家庭城市、合約市場與新秀導師共 8 條主線都補齊第四節。第三節會正式排程第四節，第四節結束後不再建立下一個待辦。
- 可見後果：故事結果頁只顯示當下已發生的結果、留下的記憶與已寫入生涯的狀態，不再提前顯示未保證發生的下一章名稱或年份。既有 pending、人物快照、隊伍／教練變動關閉規則與舊存檔相容保留。
- ONLINE：新增真正三人共用房間的 Pages Functions + D1 原型。三名玩家分別占用後衛、側翼、內線；同一回合三個位置都送出後才由伺服器結算。四回合含進攻與防守配合、衝突、共同比分與隊友信任；30 秒逾時才為缺席位置補 AI。
- 重連：房間與角色由 D1 保存，同一玩家重新加入不會取得第二個位置；前端保存最近房間代碼，重新整理後可接回原房間。結束畫面三個位置皆顯示「共同完成」，不再出現與狀態矛盾的「正在決定」。
- 資料安全：新增加法式 `migrations/0005_online_key_battle.sql`，只建立 4 張 ONLINE 原型表與索引，不改寫生涯、排行榜或既有 D1 資料。本輪只套用本機 Preview D1，正式 D1 未套用。
- 驗證：完整 193/193 tests；故事專項 41/41、ONLINE D1 E2E 3/3、D1 相容 4/4；相關 JavaScript `node --check` 與 `git diff --check` 通過。本機 Pages + D1 實際以三個玩家身分走完 4 回合，確認單人不能結算、配合結果、逾時 AI、重新加入、自動接回與共同勝負。390／430px 無可見水平溢出，主要按鈕至少 47px。
- 本機預覽：`http://127.0.0.1:8788/index.html?preview=show` 已留在瀏覽器；測試房間 `XJUYX8` 顯示共同勝利 6：1。未上傳、未合併、未部署，未修改正式版本號、README、正式 D1 或正式 `main`。

## V9.0 ONLINE SHARED WORLD 雙人／三人生涯

- 方向修正：放棄「固定同隊、每場重複三人關鍵戰」作為 ONLINE 主體。最終模型是 2／3 名玩家各自擁有獨立球員生涯，共享同一世界年份；可以不同聯盟與不同球隊，只有合理交會事件才同步停下。
- 唯一高中起點：BasketballLife 沒有美國高中或日本高校起點。所有玩家都從高一 HBL 開始，由伺服器分配不同 HBL 球隊；同一年依後衛、側翼、內線取得不同的 HBL 事件與文本。
- 高中畢業分流：HBL 三年履歷完成後，才依評價、健康與選秀聲量開放 UBA、UBA 強權、日本大學、NCAA D2、NCAA D1。每人分別保存位置、球隊、聯盟、年齡、OVR、健康、評價、選秀聲量、合約與事件後果。
- 第一輪時間線：6 個節點為 HBL 起點、高中畢業、U20 培訓觀察交會、職業入口、五場夏季聯賽、第一份合約。高中三年使年份 2026→2029、年齡 16→19；U20 後壓縮完成大學生涯，年份 2029→2032、年齡 19→22；第一個職業球季結束後進入 2033、23 歲。
- 職業延續：第二輪起不再出現 HBL 畢業或大學選擇，時間軸改為角色定位、球季轉折、跨聯盟交會、職業市場、關鍵賽段、續約決定。職業選項不再殘留「學業」文本，每季結束年份與年齡各前進一季。
- 分流與交會：職業入口可分流台灣職業市場、NBA G League 入口或日韓／海外職業市場；夏季聯賽採三場小組賽、排名賽與冠軍／合約面談的五場敘事賽制。合約依每個人的 OVR、健康、評價與選擇分別產生，各自球隊與狀態不重置。
- 雙人／三人：兩人即可由房主開始，第三人可在開局前加入。每個同步檢查點只等待真人玩家，不會由 AI 替任何人作生涯選擇；重新加入仍回到原球員與路線。
- 資料層：加法式 `migrations/0008_online_shared_world.sql` 建立共享世界、獨立玩家生涯、選擇與歷史 4 張表及索引；`origin_route` 只接受 `hbl`。已移除未採用的同隊球季／同隊劇情 migration 骨架，沒有改寫單人生涯、排行榜或既有 D1 資料。本輪只套用全新的本機 Preview D1。
- 實機驗收：本機房間 `7SNRMT` 由能人家商後衛與光富高中側翼開局；同一 2026 年分別收到「最後兩分鐘持球」與「對位對手王牌」事件。高中畢業後分流至國立體大 UBA 強權與加州紅杉大學 NCAA D2；2029 年於 U20 觀察營交會，2032 年分別進入台灣職籃與 NBA G League，2033 年第二職業球季沒有返回 HBL 或重選大學。
- 自動驗證：ONLINE D1 E2E 7/7，涵蓋雙人最低開局、HBL 位置事件、獨立後果、大學分流、年份／年齡、交會不強制同隊、夏季聯賽、獨立合約、職業續季不返回學生流程與三人等待。完整回歸 197/197，相關 JavaScript `node --check` 與 `git diff --check` 通過。
- UI：首頁與 ONLINE 頁使用 `SHARED WORLD`，同屏比較每名玩家的球隊、聯盟、OVR、健康、評價與選秀聲量；職業輪次使用職業專屬章名。390／430px 無水平溢出，主要按鈕至少 48px；390px 頁首不再把「人生涯」拆成孤字。本機預覽保留在 `http://localhost:8788/?preview=show`。
- 範圍：未上傳、未合併、未部署，未修改正式版本號、README、正式 D1 或正式 `main`。正式跨裝置多人仍需取得上傳／Preview 授權後，在 Cloudflare Preview 驗證不同網路與重連。

## 2026-08-27 發布紀錄

- PR `#64` 已合併至 `main`，merge commit `0ac2768b5f6d5f13abe57a17da627d46641d4915`。
- Cloudflare Pages 與 GitHub Pages build／deploy 均成功；正式首頁 `https://basketballlife.pages.dev/` 回應 200，已包含精華生涯、SHARED WORLD 與 `js/online/key-battle.js`。
- 正式 D1 已以加法式 SQL 建立 `0005`、`0008` 的 8 張 ONLINE 表；唯讀 schema 查詢確認全部存在。沒有更新或刪除既有生涯、排行榜資料。
- 正式 D1 的舊 `d1_migrations` metadata 仍為空；系統拒絕自動補寫歷史紀錄。未來不可直接執行全量 `wrangler d1 migrations apply`，應先人工核對並處理 `0001–0004` 的 migration 歷史。
- 發布前完整回歸 197/197，正式 API 未授權 smoke test 回傳預期 401，未建立測試玩家或測試排行榜資料。

## V9.1 正式版本與首頁入口修正

- 版本：首頁、資產快取、新生涯 `careerVersion`、本機存檔版本、公開生涯 publisher 與排行榜標題同步為 V9.1／`9.1.0`。
- 相容：V9 排名時代不拆榜；API 與前端完整性驗證同時接受 `9.0.0`、`9.1.0`，既有 V9.0 舊存檔與公開生涯仍可讀取、驗證與重新發布。V8.1 相容路徑維持不變。
- 首頁：移除建立球員卡外的全寬 SHARED WORLD 橫幅，改放在「選擇生涯節奏」內，與完整生涯、精華生涯並列為第三張入口；手機改為單欄。
- README：正式版改為 V9.1，新增精華生涯、長期故事與 SHARED WORLD 的實際玩法及限制，排行榜與 Online 說明同步更新。
- 驗證：完整回歸 197/197、相關 JavaScript `node --check`、`git diff --check` 通過；實際本機頁面桌機三卡同列，390／430px 單欄且水平溢出為 0。
- 範圍：未修改能力、合約、老化、事件機率、D1 schema 或正式資料；本節完成時尚未合併、部署。
- 中文介面：正式發布前將首頁模式入口與多人頁面的 `SHARED WORLD` 統一改為「共享世界」，入口小標改為「線上・2～3 人」；README 同步使用中文名稱。

## V9.1 共享世界完整生涯流程修正

- 根因：先前多人後端把高中到職業入口壓成固定六節點，職業輪次也使用固定六節點，因此每輪內容近似重播；前端另做一個小型多人面板，視覺與單人生涯斷裂。
- 新流程：一輪等於一個球季，固定節奏為季前訓練、一般事件、特殊事件、健康管理、球季結算、能力與生涯抉擇。一般／特殊事件依 HBL、大學、職業、老將條件池與 Seed 抽取；每三季才在第三階段插入一次合理的共享交會。
- 生涯時間：16 歲起每完成六階段只前進一歲／一年；18 歲球季結束選大學，22 歲球季結束進職業，職業後繼續逐季處理角色、低潮、影片會議、關鍵第四節、主場壓力、隊友合約年與合約市場，不再回到高中或重選大學。
- UI：多人頁改為單人生涯式球員資訊列、六階段流程列、主故事／選項區與隊友生涯側欄；桌機雙欄，760px 以下單欄。首頁仍只有「完整生涯／精華生涯／共享世界」三個中文入口。
- 相容：未新增 D1 migration、未刪除房間或排行榜資料；既有房間從目前保存的年齡、聯盟、球隊、合約與 cycle 接續，新節點開始套用逐季流程。舊版已經壓縮前進的房間不倒退年齡，以避免破壞既有玩家資料。
- 正式 UI 接回：多人模式不再顯示獨立 `onlineBattlePage`。共享世界的球員資訊、六階段流程、事件標題、結果、選項與玩家狀態直接渲染至既有單人生涯 `#game`、`#currentPanel` 與 `#choices`；只保留多人資料與同步邏輯，不另做一套生涯頁。
- 可用性修正：多人頁首不再被沒有出現的單人生涯重新開始按鈕占據第一欄；共享世界新增使用者自己的房間列表，沒有本機記憶時自動接回最近世界，切換世界時可選擇既有房間。建立上限由永久卡死的 3 間調整為 10 間，未刪除任何既有房間或 D1 資料。

## V9.1 共享世界接回正式單人生涯引擎

- 根因：前一版雖使用單人生涯外框，實際選項與數值仍由 `functions/api/key-battle.js` 的簡化事件池產生，並非正式完整／精華生涯。
- 修正：共享世界開局改為直接呼叫正式 `startCareer()`；後續沿用既有訓練、一般／特殊事件、關鍵戰、健康、結算、能力點、升學、選秀、合約、老化與退休函式。多人前端不再讀取伺服器自製 `event.options`。
- 建角：每位玩家保留自己的姓名、PG～C、身高、臂展、慣用手、出生地、球衣號碼與 Seed；房主只決定世界採完整或精華節奏。
- 同步：每人使用獨立共享世界本機存檔，不覆蓋單人存檔；球季結束時送出正式球員摘要，等待所有真人玩家完成同一年後才各自呼叫原本 `finishSeason()` 前進。
- 驗證：完整測試腳本全部通過；ONLINE 新架構 5/5、精華 13/13、事件 17/17；相關 JavaScript `node --check`、`git diff --check` 與 Wrangler 4.127.0 Pages Functions build 通過。
- 目前風險：尚未在兩台真實裝置上完成跨網路同步實玩；正式站尚未發布本節改動。
- 下一個最安全任務：建立 Preview，以兩個真人帳號走完第一個 HBL 球季與同步等待，再合併部署正式站。

## V9.1 共享同步修復與共同事件

- 卡住修復：球季同步改用 `season:<year>` checkpoint；伺服器會確認玩家列確實更新，未命中不再回傳假成功。同步重送、重新整理與其中一人先收到結果皆維持冪等，不會重複前進。
- 真正交會：兩人或三人完成合理交會球季後，各自選擇正面對決、團隊優先或交換情報；全員完成才形成共同結果，並各自套用一次評價、健康與信心後果。高中／大學每兩年、職業與跨階段每三年才停下，平淡球季直接推進，避免每季多選一次。
- 權限：非房內玩家不能送出共同事件選擇；共同結果與每人選擇寫入既有 choices/history 表，不新增 migration、不改單人生涯或排行榜資料。
- 存檔入口：按「線上・2～3 人」固定先顯示共享生涯存檔清單，不再自動跳回最後房間。等待頁新增重新同步、返回多人存檔與取消這份生涯；取消只刪除此裝置存檔並隱藏卡片，不刪隊友或伺服器房間，日後可用世界代碼重新加入。
- 驗證：完整 204/204 tests 通過；新增雙人完成、共同事件雙方選擇、先後回收結果、例行年直接前進、非成員拒絕、存檔清單與取消／重新加入測試。相關 JavaScript `node --check`、`git diff --check` 通過；390px 本機正式頁無水平溢出且載入 `careerFlow=9`／`careerFlow=5` 新資產。
- 未驗證：正式 D1 唯讀查詢因目前 Cloudflare CLI 帳號權限回傳 7403，沒有讀寫正式玩家資料；仍需 Preview／正式站以兩個真實帳號走完一次交會。
## V9.1.1 多人共同生涯完成版

- 版本：首頁資產、存檔、公開生涯 publisher、API 相容清單與快取版本更新為 `9.1.1`；V9 排行榜仍接受 `9.0.0`、`9.1.0`、`9.1.1`。README 未修改。
- 既有頁面：多人開局仍使用首頁球員建立區，遊玩與共同事件仍使用正式 `#game` 生涯頁；沒有新增替代遊戲頁。
- 共同事件：依真實階段與狀態提供 HBL 交手、招募競爭、大學邀請賽、國家培訓、職業例行賽、季後賽、冠軍戰、同隊合作、自由市場重逢、交易窗口、傷病支援、老將交棒與跨聯盟窗口等條件事件。
- 後果：所有真人選擇共同產生排名、合作分數與個別後果；隊友的施壓／支援會改變自己的健康、信心、疲勞與下一季風險。結果先在原生涯頁確認，確認後才執行原球季推進；待確認結果會隨本機存檔恢復。
- 排行榜：原排行榜新增「多人合作」分頁，依共享球季、完成共同事件與合作選擇計分；世界代碼遮蔽，且沒有共同事件的房間不進榜。
- 退休：保留每位玩家原本完整退休頁，在故事分頁追加已發生的共同生涯紀錄；退休狀態會通知共享世界，現役玩家下一次同步會收到一次性的退休送別事件。退休玩家不再卡住仍在打球的隊友，也不再被要求作答。
- 捲動：生涯畫面切換後主動把輸入焦點交回頁面；桌面精華頁不需再先點一下才能使用滑鼠滾輪。
- 驗證：`node --check` 通過；`node --test --test-isolation=none tests/*.test.mjs` 207/207 通過；`git diff --check` 通過；本機瀏覽器確認首次進入生涯時焦點為 BODY、滾輪可直接捲動，390px／430px 無水平溢位，主控台無 error。
- 發布狀態：PR #73 已合併並部署至正式站，merge commit `f6ecab848033edc4a1c62ba1278ebb46dcc45c59`。

## V9.1.1 多人事件邏輯與文本修正（本機，尚未發布）

- 觸發：季後賽與冠軍戰改讀單人生涯本季真實賽事結果，不再依年份硬猜；國家培訓要求所有參與者具備資格；傷病支援與老將交棒只在同隊時出現；學生進入職業不再誤判成職業轉隊。
- 玩家關係：跨隊比賽明確稱為真人對手；同隊合作依後衛、側翼、內線提供不同任務；傷者／健康隊友、老將／年輕球員、轉隊者／原關係球員各自看到符合身分的選項。
- 互相影響：其他玩家的施壓或支援會改變自己的健康、信心、疲勞與下一季傷病風險；同隊玩家選擇重複責任會失去合作分數並降低信心，選擇互補責任才取得完整合作結果。
- 文本邊界：不再宣稱共同事件會憑空產生招募、交易、合約、國家隊出賽或冠軍；結果頁將 `rep` 正確顯示為「外界評價」，並明示原球隊賽果、合約與去向不被改寫。
- 跨季：共同事件的 `planRisk` 改在原球季推進完成後套用，避免職業新球季初始化立即清除；使用事件 ID 保持重載冪等。
- 驗證：多人專項 22/22、完整 212/212 tests；相關 JavaScript `node --check`、`git diff --check` 通過。本機 HTTP 頁面載入 `careerFlow=11`，焦點為 BODY、水平溢出 0、主控台無 error。
- 範圍：未新增頁面、D1 migration 或正式資料寫入；未修改版本號與 README；本節尚未 commit、push、建立 PR、合併或部署。

## V9.1.1 完整多人互動系統（本機，尚未發布）

- 季中共同回合：完整生涯在一般事件中段暫停，精華生涯最晚在球季完成前暫停；2～3 名真人在原本 `#game` 生涯頁完成同一個攻防／合作回合。跨隊採進攻、封鎖、臨場調整互相克制；同隊採發動、終結、防守收尾互補，兩人需兩種責任、三人需三種責任，重複工作會形成共同失敗。
- 內容：跨季主線擴充為宿敵、升學競爭、同隊搭檔、傷病關係、老將交棒、國家培訓、真實轉隊重逢、季後賽競爭、跨聯盟距離、合約市場 10 條，每條 3 節，共 30 個跨季節點；另有 7 種條件式季中共同回合。
- 回訪：共同選擇會同步保存到所有玩家既有 `career_flags.sharedStory`；下一節直接顯示真人姓名、前次選項與目前關係，合作會開啟延續默契選項，衝突會開啟修復關係選項。
- 身分：同隊搭檔依後衛／側翼／內線分工；傷病線區分傷者與健康隊友；交棒線區分老將與年輕核心。選擇會繼續影響外界評價、健康、信心、疲勞、下季傷病風險與多人合作排行榜。
- 重新連線：季中到達、選擇與結果使用既有 cycle＋chapter 45 保存；重送不會重複加成，先完成者重新進入可取回同一結果。隊友退休後，剩餘玩家會自動略過季中等待，不會卡死。
- 多人排行榜：季中共同回合與季末主線分開計數，顯示季中回合、主線章節、共同成就、真人對決、默契與多人世界總分。
- 相容：沒有新增 D1 migration；共享狀態使用既有 `career_flags`、`online_shared_choices` 與 `online_shared_history`。舊共享存檔缺少欄位時自動套用空狀態；單人生涯、能力、合約、老化、選秀、球隊賽果與個人排行榜公式未改。20 歲已進 NBA 等職業聯盟的球員不再被年齡誤分類為大學生。
- 驗證：多人專項 32/32、完整 222/222 tests；相關 JavaScript `node --check` 與 `git diff --check` 通過。本機 HTTP 頁面載入 `careerFlow=13`，桌面／390px／430px 水平溢出為 0、可見按鈕至少 44px、焦點為 BODY、主控台無 error。
- 發布狀態：尚未 commit、push、建立 PR、合併或部署；production `main` 未變更，README 與版本號未修改。
