# BasketballLife V9.1.1 多人共享世界驗收矩陣

## 目的與合格定義

本矩陣用來驗收 2～3 人共享世界的真正多人流程。合格不是只看到隊友狀態或共享年份，而是：

1. 所有真人在同一回合做出互相依賴的選擇。
2. 伺服器只生成一個共同、權威結果。
3. 每位玩家的個人後果、存檔與事件記憶只套用一次。
4. 重新載入、斷線、重連、重複送出或不同到達順序不得重複結算。
5. 共同回合仍發生在正式完整／精華生涯頁面，不得替換成另一套簡化生涯。

## 範圍與禁止項目

- 階段：production／maintenance 的 Preview 驗收。
- 測試環境：本機 Pages Functions＋本機 D1，或隔離 Cloudflare Preview＋`basketballlife-preview` D1。
- 禁止：正式 D1 測試資料、正式排行榜測試生涯、版本號變更、為了通過驗收而改寫核心公式。
- 本文件只建立驗收方法與證據欄位；所有項目預設為「未執行」，不代表已驗證。

## 本次執行資訊

| 欄位 | 記錄 |
| --- | --- |
| 日期／時區 | 未執行 |
| 來源 commit | `7f53e58ab7290e2e8c6f36396450b4d022aba661` |
| Preview URL | 未執行 |
| D1 環境／資料庫 | 未執行 |
| 房間代碼 | 未執行 |
| 玩家 A／裝置／瀏覽器 | 未執行 |
| 玩家 B／裝置／瀏覽器 | 未執行 |
| 玩家 C／裝置／瀏覽器 | 未執行 |
| Console error | 未執行 |
| 驗收人 | 未執行 |

## 驗收前置

- [ ] `HEAD` 與本次 Preview commit 已記錄，且未混入使用者無關髒檔。
- [ ] Preview `DB` binding 指向非 Production D1。
- [ ] `0001`、`0002`、`0003`、`0004`、`0005`、`0008` 已在該 Preview D1 套用。
- [ ] `/api/health` 回應 200，並另外確認 `leaderboard_stats`、`online_key_battle_rooms`、`online_shared_worlds` 存在。
- [ ] A、B、C 使用分離瀏覽器 profile／裝置，不共用 localStorage 或玩家 token。
- [ ] 測試前記錄該房間各關鍵資料表的初始筆數。

## A. 兩人完整生涯

| ID | 情境與操作 | 預期結果 | 狀態 | 證據／問題編號 |
| --- | --- | --- | --- | --- |
| A01 | A 建立完整生涯世界，B 用 6 碼代碼加入 | 兩人看到同一房間；玩家暱稱、球員姓名與本機存檔分離 | 未執行 | |
| A02 | A／B 分別建立姓名、位置、身材、臂展、慣用手、出生地、外觀與 Seed | 兩人都從不同 HBL 球隊開始；個人設定不互相覆蓋 | 未執行 | |
| A03 | A 先抵達季中檢查點，B 繼續遊玩 | A 看到等待狀態，不能單獨結算或推進共同回合 | 未執行 | |
| A04 | B 抵達季中檢查點 | 兩人在原生涯頁看到同一件共同事件與各自適用選項 | 未執行 | |
| A05 | 同隊 A／B 選擇互補責任 | 產生一個共同成功結果；各自後果與合作分數只套用一次 | 未執行 | |
| A06 | 另一輪同隊 A／B 選擇重複責任 | 共同結果明確說明責任重疊，且結果劣於互補路徑 | 未執行 | |
| A07 | 跨隊 A／B 選擇攻防克制組合 | 雙方選擇共同決定一個對決結果；不是各自擲骰各自結算 | 未執行 | |
| A08 | A 送出選擇後重新載入，B 尚未送出 | A 仍是已送出並等待；不得重複選擇或丟失到達狀態 | 未執行 | |
| A09 | 兩人完成後，A／B 各自重新載入共同結果 | event ID、共同結果與個人數值不變；不再次加成 | 未執行 | |
| A10 | 重複送出同一回合／重複點擊 | 伺服器拒絕或回傳已有結果；選擇、歷史、年份不增加第二次 | 未執行 | |
| A11 | A 先完成季末，B 尚未完成 | A 停在季末等待；共享年份不提前增加 | 未執行 | |
| A12 | B 完成季末並觸發合理交會 | 兩人只收到一個共同結果；全員確認後年份只增加一次 | 未執行 | |
| A13 | 連續進入下一季的跨季主線 | 文本回訪同一人物與前次已發生選擇，不預告未保證章節 | 未執行 | |
| A14 | A 斷網、關閉頁面後以房間代碼重連 | 恢復 A 自己的生涯與等待／結果狀態，不覆蓋 B | 未執行 | |
| A15 | 房主封存世界後再恢復 | 封存期間禁止推進；恢復後保留年份、個人生涯、共同歷史與隊友 | 未執行 | |
| A16 | A 只從此裝置隱藏／取消本機存檔，再以代碼加入 | 伺服器世界與 B 的資料不被刪除；A 可重新加入 | 未執行 | |

## B. 三人精華生涯

| ID | 情境與操作 | 預期結果 | 狀態 | 證據／問題編號 |
| --- | --- | --- | --- | --- |
| B01 | A／B／C 以後衛、側翼、內線加入精華生涯 | 三人位置不重複，且從不同 HBL 球隊開始 | 未執行 | |
| B02 | 只有 A／B 抵達共同回合，C 尚未抵達 | 不結算、不跳過 C；等待頁正確顯示三人狀態 | 未執行 | |
| B03 | 三人分工完成後衛／側翼／內線互補責任 | 只產生一個共同成功結果，且每人看到自己實際後果 | 未執行 | |
| B04 | 至少兩人選擇同一責任 | 共同結果明確反映責任重疊，不得仍免費給予完整合作成功 | 未執行 | |
| B05 | C 已送出後斷線，A／B 完成其餘選擇，C 再重連 | C 看到同一個權威結果；個人後果只套用一次 | 未執行 | |
| B06 | 三人用不同到達順序重跑同類回合 | 到達順序不改變 Seed 決定的事件；結果只取決於有效選擇組合 | 未執行 | |

## C. 條件交會與跨季主線

| ID | 條件 | 必須確認 | 狀態 | 證據／問題編號 |
| --- | --- | --- | --- | --- |
| C01 | HBL 同年對決 | 真實球隊與年份一致；雙方選擇形成共同比賽結果 | 未執行 | |
| C02 | 招募競爭／大學邀請 | 只在玩家實際路線可相遇時觸發 | 未執行 | |
| C03 | 同聯盟職業例行賽 | 不誤用季後賽或冠軍文本 | 未執行 | |
| C04 | 季後賽／冠軍戰 | 必須來自玩家真實季後賽資格與成績 | 未執行 | |
| C05 | 培訓隊／國家隊 | 未入選或不具資格者不得出現國家隊共同故事 | 未執行 | |
| C06 | 同隊傷病支援 | 傷者與健康隊友看到不同責任；跨隊傷病不誤認為隊友支援 | 未執行 | |
| C07 | 交易、自由市場重逢 | 必須引用真實交易／市場狀態，不虛構球隊變動 | 未執行 | |
| C08 | 老將交棒 | 依實際年齡、角色與能力曲線觸發，不只看固定年齡 | 未執行 | |
| C09 | 10 條、30 節跨季主線 | 節點順序、人物身分、既有選擇與回訪文字一致 | 未執行 | |

## D. 退休、共同終章與多人排行

| ID | 情境與操作 | 預期結果 | 狀態 | 證據／問題編號 |
| --- | --- | --- | --- | --- |
| D01 | A 先退休，B／C 繼續遊玩 | A 不再卡住共同回合；B／C 保留原生涯、共享年份與已有歷史 | 未執行 | |
| D02 | 退休玩家重新載入／查看世界 | 可查看自己告別與共同紀錄，不得恢復現役選擇 | 未執行 | |
| D03 | 最後一人退休 | 世界只完成一次，共同終章包含年份、球員、共同事件、合作、成就與對決 | 未執行 | |
| D04 | 所有玩家在終章後重新載入 | 共同終章內容不變；房間保持完成，不可再恢復推進 | 未執行 | |
| D05 | 重複載入、重複查看、重送最後結果 | `online_shared_history` 唯一事件數不變；多人合作排行結果不重複增加 | 未執行 | |
| D06 | 查看多人合作排行 | 分數來自真正完成的共同回合、主線、共同成就、真人對決與默契，不直接把單人數值當合作分數 | 未執行 | |

## E. 裝置、可用性與失敗路徑

| ID | 情境與操作 | 預期結果 | 狀態 | 證據／問題編號 |
| --- | --- | --- | --- | --- |
| E01 | 桌機瀏覽器完整流程 | 無 Console error；滾輪、點擊、焦點與等待更新正常 | 未執行 | |
| E02 | 390px 手機視窗 | 無水平溢出；按鈕可點；文字不裁切；等待與共同結果可捲動 | 未執行 | |
| E03 | 430px 手機視窗 | 無水平溢出；按鈕可點；文字不裁切；不覆蓋原生涯導覽 | 未執行 | |
| E04 | 手機 Safari 真機重連 | 頁面背景／恢復後可取得最新權威狀態，不重複套用結果 | 未執行 | |
| E05 | 鍵盤操作與焦點 | 建立、加入、選擇、確認、重試皆可以鍵盤完成，結果後焦點位於可操作的下一步 | 未執行 | |
| E06 | API 短暫中斷／延遲／重複回應 | 顯示可理解的等待或重試狀態；不加倍結果、不丟失本機生涯 | 未執行 | |

## Preview D1 證據

將 `<ROOM_CODE>` 替換為本次房間代碼，只能對隔離 Preview D1 執行。

```sql
SELECT room_code, status, current_turn, updated_at
FROM online_key_battle_rooms
WHERE room_code = '<ROOM_CODE>';

SELECT cycle_number, chapter_number, COUNT(*) AS player_choices
FROM online_shared_choices
WHERE room_code = '<ROOM_CODE>'
GROUP BY cycle_number, chapter_number
ORDER BY cycle_number, chapter_number;

SELECT cycle_number,
       chapter_number,
       COUNT(*) AS player_rows,
       COUNT(DISTINCT event_id) AS authoritative_events
FROM online_shared_history
WHERE room_code = '<ROOM_CODE>'
GROUP BY cycle_number, chapter_number
ORDER BY cycle_number, chapter_number;
```

共同回合預期每位玩家最多一筆 choice，而同一 cycle／chapter 的 `authoritative_events` 預期為 1。重新載入、重複送出與重連後重跑查詢，數量不得額外增加。

## 問題記錄格式

發現問題時先記錄，不直接重寫多人系統：

```text
問題 ID：MP-
嚴重度：P0／P1／P2／P3／P4
來源 commit：
Preview URL／D1：
房間代碼：
玩家／裝置／瀏覽器：
前置狀態：
重現步驟：
實際結果：
預期結果：
Console／API／D1 證據：
截圖／錄影：
根因假設：
可否證的最小測試：
重跑範圍：
```

## 完成條件

- 兩人完整生涯、三人精華生涯、提早退休與共同終章的所有必要項目已有 PASS／FAIL 與證據。
- 每個共同結果的唯一性均有 UI、API 與 Preview D1 三層證據。
- 斷線、重連、重複送出、不同到達順序與重新載入已驗收。
- 桌機、390px、430px 與手機 Safari 真機結果已分開記錄。
- 沒有任何測試記錄寫入 Production D1。
- 未通過項目已轉成可重現問題，並以最小根因假設排序修正。
